{
  "smart-TZ/vitest.config.ts": "import { Config } from 'vitest';

const config: Config = {
  testEnvironment: 'node',
  testMatch: ['**/*.test.ts'],
  setupFilesAfterEnv: [],
};

export default config;"
}